cmake_minimum_required(VERSION 3.10)
project(GNSS_Localizer_Standalone)

set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED ON)

# Optional: extra include/library paths
# include_directories(include)
# find_package(Eigen3 REQUIRED)
# find_package(GeographicLib REQUIRED)

# Sources discovered automatically
add_executable(gnss_localizer

)

# Optional link libraries
# target_link_libraries(gnss_localizer PRIVATE Eigen3::Eigen GeographicLib)
